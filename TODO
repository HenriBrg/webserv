Actualisée au 31/10/2020 :

CONFIG :

    - Gérer les cas d'erreurs dans la configuration
    - Ajouter l'autoindex on/off dans chaque location
    - stress test config 
        -> segfault si le path root est érroné
        ->
    - une ligne de log par serveur après parsing config ?
    - erreur si aucune methode présente ?

CGI :

    - POST

REQ/RES :

    - Gérer autoindex
    - Gérer le fichier erreur + remplacement variable
    - Gérer le matching fichier par content-language : index.html.fr
    - retry-after, check les headers non utilsés et voir si normal ou pas
    - gérer max file descriptor
    - Parsing des headers en plusieurs recv() à gérer (telnet, netcat)
    - response en chunked
    - max longueur uriQueries (255 je crois)

GLOBAL :

    - Testeur Shell ou Python
    - Leaks mémoire / socket
    - S'assurer de bien tout reset entre chaque client, si un même client fait plusieurs requêtes
    - Ejecter le client au bout d'une minute ?

Tous les TODO en commentaire visible via :

    - grep -rin TODO  srcs/*.cpp